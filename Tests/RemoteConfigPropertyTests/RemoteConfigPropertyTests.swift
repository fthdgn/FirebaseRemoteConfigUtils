import XCTest
@testable import RemoteConfigProperty

final class RemoteConfigPropertyTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RemoteConfigProperty().text, "Hello, World!")
    }
}
